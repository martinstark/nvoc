[package]
name = "nvoc"
version = "0.2.0"
edition = "2021"
description = "NVIDIA Blackwell GPU overclocking utility for Linux"
license = "MIT"
keywords = ["nvidia", "gpu", "overclocking", "nvml", "blackwell"]
categories = ["command-line-utilities", "hardware-support"]

[[bin]]
name = "nvoc"
path = "src/main.rs"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
libc = "0.2"
libloading = "0.8"
regex = "1.12"
